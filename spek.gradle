apply plugin: 'org.junit.platform.gradle.plugin'

junitPlatform {
    engines {
        include 'spek'
    }
}

dependencies {
    testCompile lib.spek["api"], lib.spek["expekt"]
    testCompile lib.junit["platform"]
    testRuntime lib.spek["junit"]
}